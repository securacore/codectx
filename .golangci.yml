version: "2"

run:
  timeout: 3m

linters:
  default: standard
  enable:
    - misspell

  settings:
    misspell:
      locale: US

formatters:
  enable:
    - gofmt

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
